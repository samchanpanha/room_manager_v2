/**
 * Core kernel (INTENT.md M00): the foundation every module depends on —
 * Party model, Settings, NumberSequences, AuditLog, DomainEvents, and Tenant.
 * Shared module.
 */
@org.springframework.modulith.ApplicationModule
package com.rentmanager.kernel;
